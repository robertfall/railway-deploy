# Simple Wallboard for Railway Projects

Designed to run fullscreen on a large display in a public area.

## Development

Run the Vite dev server:

```shellscript
npm run dev
```

## Deployment

First, build your app for production:

```sh
npm run build
```

Then run the app in production mode:

```sh
npm start
```